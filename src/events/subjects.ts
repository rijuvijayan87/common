export enum Subjects {
  TicketCreated = 'ticket:created',
  TicketUpdated = 'ticket:updated',

  OrderCreated = 'order:cancelled',
  OrderCancelled = 'order:cancelled',

  ExpirationComplete = 'expiration:complete',
}
